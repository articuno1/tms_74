<% provide(:title, @user.username) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= image_tag @user.avatar.url %>
        <%= @user.username %>
      </h1>
      <%= link_to t(".edit"), edit_user_path(current_user), class: "fa fa-pencil-square-o btn btn-info" %>
    </section>
  </aside>
</div>
